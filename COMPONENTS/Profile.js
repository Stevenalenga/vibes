import React from "react";
import { Text, StyleSheet } from "react-native";

const Profile = () => {
  return <Text>hi this is you ;</Text>;
};

const styles = StyleSheet.create({
  Text: {
    justifyContent: "center",
  },
});

export default Profile;
